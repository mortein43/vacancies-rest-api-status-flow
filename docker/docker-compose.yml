services:
  backend:
    build:
      context: ../backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_HOST: db
      DATABASE_PORT: 5432

  frontend:
    build:
      context: ../frontend
    ports:
      - "3001:80"
    environment: VITE_API_URL=http://46.62.146.164:8000

  db:
    image: postgres:15
    ports:
      - "5432:5432"
